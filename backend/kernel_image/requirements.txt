# Pinned data-science stack for the sandboxed kernel image (MASTER_PROMPT.md §2, §9).
#
# This is a separate process from the FastAPI backend, so its pins don't need to match
# backend/pyproject.toml. In particular pandas/numpy are pinned to the 2.x/1.26 line here
# (rather than the backend's pandas 3.0.5 / numpy 2.5.3) because ydata-profiling has no
# pandas-3-compatible release yet, and that in turn caps scipy/numpy transitively.
# Exported notebooks (MASTER_PROMPT.md §6) are plain, standalone notebooks that only need
# to run with these pinned kernel-image versions, not the backend's.
ipykernel==6.29.5
matplotlib-inline==0.1.7
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0
polars==1.9.0
openpyxl==3.1.5
charset-normalizer==3.5.1
scipy==1.13.1
statsmodels==0.14.4
scikit-learn==1.5.2
imbalanced-learn==0.12.4
category_encoders==2.6.4
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1
shap==0.46.0
ydata-profiling==4.12.2
joblib==1.4.2
